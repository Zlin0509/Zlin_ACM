#include <bits/stdc++.h>
using namespace std;

int pre[200010]{};

void solve()
{
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T;
    cin >> T;
    while (T--)
    {
        solve();
    }
}