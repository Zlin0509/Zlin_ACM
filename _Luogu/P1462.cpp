#include <bits/stdc++.h>
using namespace std;
const int N = 2e5 + 5;
int tot = 0, head[N];
struct edge
{
    int to, next, val, w;
} e[N];

inline void Zlin()
{
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
}