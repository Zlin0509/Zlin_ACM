#include <bits/stdc++.h>
using namespace std;

int n, k;
char s[200010]{};

void solve()
{
    cin >> n >> k;
    cin >> s;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T;
    cin >> T;
    while (T--)
        solve();
}